mutation CreateProduct($input: CreateProductInput!) {
    createProduct(input: $input) {
        id
        companyId
        sku
        brand
        model
        categoryId
        typeId
        description
        detailsData
        affectsInventory
        createdAt
    }
}
